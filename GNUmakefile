include $(GNUSTEP_MAKEFILES)/common.make

APP_NAME = Editor

Editor_HEADERS = Document.h

Editor_OBJC_FILES = main.m \
                   Document.m

Editor_RESOURCE_FILES = EditorInfo.plist \
                       Editor.gorm \
                       Document.gorm

Editor_MAIN_MODEL_FILE = Editor.gorm

include $(GNUSTEP_MAKEFILES)/application.make

